// let img;

function preload(){
  // img - loadImage('kitten.jpg')
}

function setup() {
  createCanvas(400, 400,)
  background(200) 
}

function draw() {
  // image(img, 0, 0, mouseX, mouseY)
  // ellipse(0, 0, 100, 100)
}